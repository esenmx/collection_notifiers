name: collection_notifiers
description: Collections with implementation of ValueListenable/ChangeNotifier for minimum rebuild and sugared syntax
version: 1.0.1
homepage: http://mehmetesen.com
repository: https://github.com/esenmx/collection_notifiers/

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter
  collection: ^1.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  mockito: ^5.1.0
  test_utils:
    git:
      url: https://github.com/esenmx/test_utils
      ref: main

flutter:
