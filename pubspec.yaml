name: collection_notifiers
description: Collections with implementation of ChangeNotifier/ValueListenable for minimum rebuild and sugared syntax
version: 1.0.3
homepage: http://mehmetesen.com
repository: https://github.com/esenmx/collection_notifiers/

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  collection: ">=1.16.0 <2.0.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
  mockito: ">=5.2.0 <6.0.0"
  test_utils:
    git:
      url: https://github.com/esenmx/test_utils
      ref: main

flutter:
